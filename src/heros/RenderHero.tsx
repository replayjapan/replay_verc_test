import React from 'react'

// Hero rendering removed in M22 — Pages no longer have a hero field.
// This file is kept as dead code for reference. HeroCarousel block is used instead.

type HeroProps = Record<string, unknown>

export const RenderHero: React.FC<HeroProps> = () => {
  return null
}
